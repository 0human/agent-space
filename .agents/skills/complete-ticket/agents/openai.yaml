interface:
  display_name: "Complete Ticket"
  short_description: "Verify criteria, merge the implementation PR, and close the ticket"
  default_prompt: "Use $complete-ticket to verify and complete the specified ticket."

policy:
  allow_implicit_invocation: false
