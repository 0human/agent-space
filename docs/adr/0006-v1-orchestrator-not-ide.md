# V1 作为 Workflow 编排器而不是代码编辑器

V1 的 APP 聚焦 Workflow、Skill、上下文、Artifact、审批和执行状态，不实现完整代码编辑器；代码编辑交由用户选择的外部 IDE。内置 Workflow 保持只读，用户通过复制生成 Project Workflow，只支持有限配置；完整的可视化 Workflow Builder 和任意图编辑留到后续版本。这样首版可以验证从 Idea 到交付的开发闭环，而不会同时承担 IDE 和通用流程设计器的复杂度。

## Status

accepted
